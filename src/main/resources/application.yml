spring:
  profiles:
    active: dev
  datasource:
    url: jdbc:mysql://localhost:3306/read_history_wisely?useSSL=false&serverTimezone=Asia/Shanghai
#    username: 'O&M'
#    password: 'O&M123456'
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  redis:
    host: localhost
    port: 6379
    password:
    timeout: 5000
    lettuce:
      pool:
        max-active: 10
        max-idle: 10
        min-idle: 1

# SSO服务器配置
sso:
  server:
    url: http://localhost:10000  # SSO认证中心地址
  # 应用域名配置
  app:
    domain: http://localhost:10050  # 当前应用域名
reading:
  app:
    domain: http://localhost:10000/edit.html  # 本地开发的阅读页域名

# DeepSeek AI 配置
ai:
  deepseek:
    api-key: sk-df236c65f526479f99d49aa538f429b4
    base-url: https://api.deepseek.com
    model: deepseek-chat
